% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compartment_coassoc.R
\name{compartment_coassoc}
\alias{compartment_coassoc}
\title{Compartment co-association with density-normalized proximity}
\usage{
compartment_coassoc(
  df,
  px = 0.149,
  ci.cutoff = 0,
  alpha_pair = 0.05,
  alpha_cross = 0.05,
  show_progress = TRUE,
  title_rel_height = 0.08,
  return_exemplars = TRUE
)
}
\arguments{
\item{df}{A data frame with columns:
`genotype`, `well`, `field`, `unique.cell`, `object.id`,
`object.type` (exactly two unique values), `x.coord`, `y.coord`,
`corr.intensity`.}

\item{px}{Numeric. Microns per pixel (default 0.149).}

\item{ci.cutoff}{Numeric scalar or vector. Global corrected-intensity cutoff(s).}

\item{alpha_pair}{Numeric in (0,1). Significance to merge same-type puncta into compartments.}

\item{alpha_cross}{Numeric in (0,1). Significance for cross-type proximity (src -> tgt).}

\item{show_progress}{Logical. Show CLI progress bars (default TRUE).}

\item{title_rel_height}{Numeric. Relative height of title row in cowplot grid.}

\item{return_exemplars}{Logical. If TRUE, returns exemplar cell coordinate lists.}
}
\value{
If `length(ci.cutoff) == 1`, a list with:
  - `obj1_name`, `obj2_name` (character)
  - `well_summary` (long, per-well table)
  - `final.plot` (cowplot/ggplot object)
  - `exemplars` (nested list: readout -> genotype -> list of up to 5 data frames)

  If `length(ci.cutoff) > 1`, a named list of such lists (names like `"ci=2"`),
  with attributes `"obj1_name"` and `"obj2_name"`.
}
\description{
Build per-cell "positive compartments" for two object types, test cross-type
proximity against density-derived random expectation, summarize by well and
genotype, and return a plot plus exemplar cells closest to genotype means.
}
\examples{
\dontrun{
  res <- ndrutils::compartment_coassoc(df, px = 0.149, ci.cutoff = c(0, 2))
  res$`ci=2`$final.plot
}

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compartment_coassoc2.R
\name{compartment_coassoc2}
\alias{compartment_coassoc2}
\title{Compartment co-association with density-normalized proximity
(DT core + null_model + geno_fct + palette/base_size)}
\usage{
compartment_coassoc2(
  df,
  px = 0.149,
  ci.cutoff = 0,
  alpha_pair = 0.05,
  alpha_cross = 0.05,
  show_progress = TRUE,
  title_rel_height = 0.08,
  return_exemplars = TRUE,
  null_model = c("global", "local_kde", "random_labelling"),
  bw = "silverman",
  bw_frac = 0.12,
  kde_n = 128,
  n_kde_min = 10,
  kde_shrink_k = 20,
  n_sim = 199,
  seed = NULL,
  rl_max_comp_per_cell = 400,
  progress_every = 50,
  geno_fct = NULL,
  palette = 1L,
  base_size = 8
)
}
\arguments{
\item{df}{A data frame with columns:
`genotype`, `well`, `field`, `unique.cell`, `object.id`,
`object.type` (exactly two unique values), `x.coord`, `y.coord`,
`corr.intensity`.}

\item{px}{Numeric. Microns per pixel (default 0.149).}

\item{ci.cutoff}{Numeric scalar or vector. Global corrected-intensity cutoff(s).}

\item{alpha_pair}{Numeric in (0,1). Significance to merge same-type puncta into compartments.}

\item{alpha_cross}{Numeric in (0,1). Significance for cross-type proximity (src -> tgt).}

\item{show_progress}{Logical. Show CLI progress bars (default TRUE).}

\item{title_rel_height}{Numeric. Relative height of title row in cowplot grid.}

\item{return_exemplars}{Logical. If TRUE, returns exemplar cell coordinate lists.}

\item{null_model}{Character. One of "global", "local_kde", or "random_labelling".}

\item{bw}{Character or function. Bandwidth method: "silverman", "scott", "frac", or a custom function.}

\item{bw_frac}{Numeric. Fraction of cell diameter for bandwidth when bw="frac".}

\item{kde_n}{Integer. Grid size for KDE (default 128).}

\item{n_kde_min}{Integer. Minimum points for KDE (default 10).}

\item{kde_shrink_k}{Numeric. Shrinkage parameter for local KDE (default 20).}

\item{n_sim}{Integer. Number of permutations for random_labelling (default 199).}

\item{seed}{Integer. Random seed for reproducibility.}

\item{rl_max_comp_per_cell}{Integer. Max compartments per cell for random_labelling.}

\item{progress_every}{Integer. Update progress bar every N cells.}

\item{geno_fct}{Character vector. Factor levels for genotype ordering.}

\item{palette}{Integer. Palette ID for theme_ndrutils (default 1).}

\item{base_size}{Numeric. Base font size for plots (default 8).}
}
\value{
If `length(ci.cutoff) == 1`, a list with:
  - `obj1_name`, `obj2_name` (character)
  - `well_summary` (long, per-well table)
  - `final.plot` (cowplot/ggplot object)
  - `exemplars` (nested list: readout -> genotype -> list of up to 5 data frames)

  If `length(ci.cutoff) > 1`, a named list of such lists (names like `"ci=2"`),
  with attributes `"obj1_name"` and `"obj2_name"`.
}
\description{
Fast DT-only internals; configurable null model ("global", "local_kde", "random_labelling");
genotype factor ordering via `geno_fct`; and plot styling via `palette` and `base_size`.
}
